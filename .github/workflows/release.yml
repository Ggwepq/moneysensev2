name: Production Release

on:
  pull_request:
    types: [closed]
    branches:
      - test-master

jobs:
  build-and-release:
    # Only run if the PR was merged
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetches all tags so we can check if the version exists

      # 1. Extract Version from PR Title
      - name: Extract version
        id: get_version
        run: |
          TITLE="${{ github.event.pull_request.title }}"
          VERSION=$(echo "$TITLE" | grep -oP 'v[0-9]+\.[0-9]+\.[0-9]+' | head -1)
          
          if [ -z "$VERSION" ]; then
            echo "ERROR_MSG=No version pattern (e.g. v2.1.1) found in PR title." >> $GITHUB_ENV
            echo "SKIP_RELEASE=true" >> $GITHUB_ENV
          else
            echo "VERSION=$VERSION" >> $GITHUB_ENV
            echo "SKIP_RELEASE=false" >> $GITHUB_ENV
          fi

      # 2. Check if Tag Already Exists
      - name: Check if tag exists
        id: check_tag
        if: env.SKIP_RELEASE == 'false'
        run: |
          if git rev-parse "${{ env.VERSION }}" >/dev/null 2>&1; then
            echo "TAG_EXISTS=true" >> $GITHUB_ENV
            echo "Version ${{ env.VERSION }} already exists. Skipping build."
          else
            echo "TAG_EXISTS=false" >> $GITHUB_ENV
          fi

      # 3. Setup Flutter (Only if version is new)
      - name: Setup Flutter
        if: env.SKIP_RELEASE == 'false' && env.TAG_EXISTS == 'false'
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        if: env.SKIP_RELEASE == 'false' && env.TAG_EXISTS == 'false'
        run: flutter pub get

      # 4. Build the APK
      - name: Build APK
        if: env.SKIP_RELEASE == 'false' && env.TAG_EXISTS == 'false'
        run: flutter build apk --release

      # 5. Rename the APK
      - name: Rename Artifact
        if: env.SKIP_RELEASE == 'false' && env.TAG_EXISTS == 'false'
        run: mv build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/moneysense-universal.apk

      # 6. Create GitHub Release & Upload Asset
      - name: Create Release
        if: env.SKIP_RELEASE == 'false' && env.TAG_EXISTS == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          name: Release ${{ env.VERSION }}
          generate_release_notes: true
          files: build/app/outputs/flutter-apk/moneysense-universal.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Optional: Log why it skipped
      - name: Log skip reason
        if: env.SKIP_RELEASE == 'true' || env.TAG_EXISTS == 'true'
        run: echo "Release process skipped. Reason: ${{ env.ERROR_MSG }}${{ env.TAG_EXISTS == 'true' && 'Version already exists' || '' }}"