/// Identifies a specific feature tutorial.
///
/// Add a new value here + a matching case in [TutorialRouteHelper] to create
/// a new tutorial — everything else (push animation, back gesture, scaffold)
/// is handled automatically.
enum TutorialRoute { denominationVibration, shakeToGoBack, gesturalNavigation }
